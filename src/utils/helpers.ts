/**
 * BACKWARD-COMPAT SHIM — sera supprimé en Phase 9.
 */
export {
  ensureDir,
  fileExists,
  formatDate,
  generateId,
  getConfigPath,
  getCronJobsPath,
  getHomeDir,
  getMemoryDir,
  getSkillsDir,
  truncate,
} from "../shared/helpers.ts";

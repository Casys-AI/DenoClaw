services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: denoclaw
      POSTGRES_USER: denoclaw
      POSTGRES_PASSWORD: denoclaw
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

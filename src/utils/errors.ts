/**
 * BACKWARD-COMPAT SHIM — sera supprimé en Phase 9.
 * Ré-exporte depuis src/shared/errors.ts.
 */
export { ChannelError, ConfigError, DenoClawError, ProviderError, ToolError } from "../shared/errors.ts";
